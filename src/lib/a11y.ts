/** Shared a11y / focus utility classes */

/** Visible keyboard focus ring — light surfaces */
export const focusRing =
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ink/35 focus-visible:ring-offset-2 focus-visible:ring-offset-canvas";

/** Visible keyboard focus ring — dark / cinematic surfaces */
export const focusRingDark =
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-canvas/70 focus-visible:ring-offset-2 focus-visible:ring-offset-deep";

/** Minimum touch target (44×44) */
export const touchTarget = "min-h-11 min-w-11";
